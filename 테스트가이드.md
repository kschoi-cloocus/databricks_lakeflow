# 프로토타입 직접 테스트 가이드 (스텝별)

이 문서는 이전에 만든 Lakeflow SDP 프로토타입 산출물을 **직접 따라 실행/검증**하기 위한 단계별 안내입니다.

## 대상 정보 (한눈에)

| 항목 | 값 |
|---|---|
| 워크스페이스 URL | https://adb-7405617890852498.18.azuredatabricks.net |
| CLI 프로파일 | `DEFAULT` (계정 최광석 / kschoi@cloocus.com) |
| 파이프라인 이름 | `sdp_dev_dynamic_yaml` |
| 파이프라인 ID | `367220ef-f40a-416a-8932-adb5839d470b` |
| 카탈로그.스키마 | `adb_wrkspc_krc_dev.sdp_poc_demo` |
| SQL 웨어하우스 | `Serverless Starter Warehouse` (id `e6d81062ddc9efdc`) |
| 설정(YAML) | `/Volumes/adb_wrkspc_krc_dev/sdp_poc_demo/cfg/pipelines.yaml` |

> **두 가지 길**: 🖱️ **UI**(클릭, 가장 쉬움) 또는 ⌨️ **CLI/SQL**(정확한 명령). 각 스텝에 둘 다 표기.

---

## STEP 0 — 준비 확인

**⌨️ CLI**
```bash
databricks --version                       # CLI 설치 확인
databricks auth profiles                    # DEFAULT 가 Valid=YES 인지
databricks current-user me -p DEFAULT       # 로그인 계정 확인
```
- `DEFAULT` 가 `adb-7405617890852498.18` 호스트를 가리키면 정상.
- 토큰 만료 시: `databricks auth login --host https://adb-7405617890852498.18.azuredatabricks.net`

**🖱️ UI**: 위 워크스페이스 URL 로그인.

---

## STEP 1 — 파이프라인 살펴보기 (DAG 그래프)

**🖱️ UI**
1. 왼쪽 사이드바 **Jobs & Pipelines**(또는 Workflows) → **Pipelines** 탭
2. `sdp_dev_dynamic_yaml` 클릭
3. 가운데 **그래프(DAG)** 에서 bronze → silver → gold, streaming, SCD, 장애 브랜치의 의존성이 자동으로 연결된 모습 확인

**⌨️ CLI**
```bash
databricks pipelines get 367220ef-f40a-416a-8932-adb5839d470b -p DEFAULT
```

✅ 확인 포인트: 순서를 코드에 안 적었는데도 SDP가 **의존성/순서를 자동 계산**했다는 점.

---

## STEP 2 — 결과 테이블 둘러보기 (13개)

**🖱️ UI**: 사이드바 **Catalog** → `adb_wrkspc_krc_dev` → `sdp_poc_demo` → 테이블 목록. 각 테이블 클릭 → **Sample Data** 탭.

**⌨️ CLI**
```bash
databricks tables list adb_wrkspc_krc_dev sdp_poc_demo -p DEFAULT
```

**🔎 SQL** (사이드바 **SQL Editor**, 웨어하우스 = Serverless Starter 선택 후 실행)
```sql
USE CATALOG adb_wrkspc_krc_dev;  USE SCHEMA sdp_poc_demo;

-- 계층별 행수 (품질 격리 확인: bronze = silver + quarantine)
SELECT 'bronze' t, count(*) n FROM bronze_trips
UNION ALL SELECT 'silver', count(*) FROM silver_trips_clean
UNION ALL SELECT 'quarantine', count(*) FROM quarantine_trips;

-- 동적 생성된 gold
SELECT * FROM gold_by_fare_band ORDER BY dimension_value;

-- CDC 결과
SELECT * FROM customer_scd1 ORDER BY id;                         -- 최신 상태
SELECT id,name,city,__START_AT,__END_AT FROM customer_scd2 ORDER BY id; -- 이력
```
✅ 확인: `bronze 21,932 = silver 21,922 + quarantine 10` (데이터 손실 0).

---

## STEP 3 — 파이프라인 재실행 관찰

**🖱️ UI**: 파이프라인 화면 우상단 **Start** 클릭 → DAG 노드가 파랑(실행)→초록(완료)으로 변하는 과정을 실시간 관찰.

**⌨️ CLI**
```bash
databricks pipelines start-update 367220ef-f40a-416a-8932-adb5839d470b -p DEFAULT
# 상태 확인 (COMPLETED 될 때까지 몇 번 실행)
databricks pipelines get 367220ef-f40a-416a-8932-adb5839d470b -p DEFAULT \
  | python3 -c "import sys,json;print(json.load(sys.stdin)['latest_updates'][0]['state'])"
```

---

## STEP 4 — 시나리오 직접 재현 (핵심 · SQL로 손쉽게)

> 모든 소스 테이블은 `sdp_poc_demo` 안에 있어 SQL Editor에서 바로 수정 가능. 수정 → STEP 3처럼 **재실행** → 결과 조회.

### 4a. 증분 처리 ⑤⑥
```sql
-- (1) 소스에 50행 추가
INSERT INTO raw_trips_landing SELECT * FROM samples.nyctaxi.trips LIMIT 50 OFFSET 2000;
-- (2) 파이프라인 재실행(STEP 3) 후
SELECT count(*) FROM streaming_trips;                 -- 이전보다 +50
DESCRIBE HISTORY streaming_trips;                     -- 최신 write 의 numOutputRows = 50 (신규만!)
```
✅ 전량 재처리가 아니라 **신규분만** 처리됨.

### 4b. 파라미터화 ⑧
**🖱️ UI**: 파이프라인 → **Settings**(톱니) → **Advanced/Configuration** → `demo.min_fare` 값을 `50`→`100` 으로 변경 → Save → Start.
```sql
SELECT count(*) FROM gold_high_fare;                  -- 값 바꿀 때마다 행수 변화
```

### 4c. CDC / SCD ⑦
```sql
-- 변경 3건 주입 (seq는 기존보다 큰 값)
INSERT INTO customer_cdc VALUES
  (2,'Bob','Jeju','UPDATE',3), (4,'Dave','Ulsan','DELETE',3), (5,'Erin','Seoul','INSERT',3);
-- 재실행 후
SELECT * FROM customer_scd1 ORDER BY id;              -- 최신: Bob=Jeju, Dave 삭제, Erin 추가
SELECT id,name,city,__START_AT,__END_AT FROM customer_scd2 ORDER BY id,__START_AT; -- 이력 누적
```

### 4d. 장애(fail-fast) + 복구 ⑩
```sql
-- (1) 불량 데이터 주입 (규칙: amount >= 0)
INSERT INTO orders_raw VALUES (999,'Bad',-99.0);
```
→ 재실행하면 **update 가 FAILED** (UI 에서 빨강). 이벤트로그/알림으로 포착됨.
```sql
-- (2) 복구: 불량 제거 후 재실행 → COMPLETED
DELETE FROM orders_raw WHERE amount < 0;
```

### 4e. 동적 확장 ①③ (선택 · YAML 편집)
설정 파일을 내려받아 gold 한 줄 추가 후 업로드:
```bash
# 다운로드
databricks fs cp dbfs:/Volumes/adb_wrkspc_krc_dev/sdp_poc_demo/cfg/pipelines.yaml ./pipelines.yaml -p DEFAULT
# 편집기로 gold_tables 아래에 한 항목 추가, 예:
#   - name: gold_by_payment
#     dimension: payment_type   (샘플에 없으면 임의 컬럼/CASE 식)
# 업로드
databricks fs cp ./pipelines.yaml dbfs:/Volumes/adb_wrkspc_krc_dev/sdp_poc_demo/cfg/pipelines.yaml -p DEFAULT --overwrite
```
→ 재실행하면 **새 gold 테이블이 자동 생성**됨.

---

## STEP 5 — 모니터링 확인

**🖱️ UI**: 파이프라인 화면 → 하단/우측 **Run/Update 이력**, 각 테이블 노드 클릭 시 **Data quality** 지표.

**🔎 SQL** (이벤트로그 UC 테이블)
```sql
USE CATALOG adb_wrkspc_krc_dev;  USE SCHEMA sdp_poc_demo;

-- (1) 실행 상태 이력
SELECT timestamp, details:update_progress.state AS state
FROM pipeline_event_log
WHERE details:update_progress.state IS NOT NULL
ORDER BY timestamp DESC LIMIT 10;

-- (2) 플로우(테이블)별 최종 출력 행수
SELECT origin.flow_name AS flow, details:flow_progress.metrics.num_output_rows AS rows
FROM pipeline_event_log
WHERE event_type='flow_progress' AND details:flow_progress.status='COMPLETED'
      AND details:flow_progress.metrics.num_output_rows IS NOT NULL
QUALIFY row_number() OVER (PARTITION BY origin.flow_name ORDER BY timestamp DESC)=1
ORDER BY flow;

-- (3) 데이터 품질 (expectation 통과/실패)
SELECT DISTINCT details FROM pipeline_event_log
WHERE details LIKE '%"expectations"%' AND details LIKE '%passed_records%' LIMIT 3;

-- (4) 실패 포착
SELECT timestamp, substr(message,1,160) msg FROM pipeline_event_log
WHERE level='ERROR' ORDER BY timestamp DESC LIMIT 5;
```

---

## 되돌리기 (초기 상태로 복구)

시나리오를 여러 번 돌린 뒤 깨끗한 상태로 돌리려면:
```sql
-- 소스 초기화
CREATE OR REPLACE TABLE raw_trips_landing AS SELECT * FROM samples.nyctaxi.trips LIMIT 1000;
CREATE OR REPLACE TABLE customer_cdc (id INT, name STRING, city STRING, op STRING, seq INT);
INSERT INTO customer_cdc VALUES (1,'Alice','Seoul','INSERT',1),(2,'Bob','Busan','INSERT',1),(3,'Carol','Daegu','INSERT',1);
CREATE OR REPLACE TABLE orders_raw (order_id INT, customer STRING, amount DOUBLE);
INSERT INTO orders_raw VALUES (101,'Alice',120),(102,'Bob',80),(103,'Carol',50),(104,'Dave',200),(105,'Erin',30);
```
그 후 파이프라인을 **Full refresh**로 재실행:
```bash
databricks pipelines start-update 367220ef-f40a-416a-8932-adb5839d470b --full-refresh -p DEFAULT
```

---

## 팁 & 주의

- 스트리밍 소스(`raw_trips_landing`)를 `CREATE OR REPLACE` 로 바꾸면 체크포인트 충돌이 나므로, 재실행 시 **--full-refresh** 사용.
- SQL Editor 첫 쿼리는 서버리스 웨어하우스가 뜨는 데 20~30초 걸릴 수 있음.
- 개발 모드(현재)는 장애 시 즉시 실패(디버깅 용이), 프로덕션 모드(`development=false`)는 자동 재시도.
